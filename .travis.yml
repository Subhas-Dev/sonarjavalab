sudo: false
install: true

addons:
  sonarcloud:
    organization: "subhas-dev"
    token:
      secure: "caac2b9b2879d14a3eb37be6cc4533cb390b2456"

script:
  # JaCoCo is used to have code coverage, "-Pcoverage" activates the maven profile in the pom.xml
  - mvn clean verify sonar:sonar 

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'

